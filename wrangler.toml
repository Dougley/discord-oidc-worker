name = "discord-oidc"
main = "worker.js"
compatibility_date = "2025-06-01"

# Non-sensitive configuration. CLIENT_SECRET and SIGNING_KEY are secrets — set them
# with `npx wrangler secret put <NAME>` (do NOT put them here).
[vars]
CLIENT_ID = "1219413673861189642"
REDIRECT_URL = "https://dougley.cloudflareaccess.com/cdn-cgi/access/callback"
SERVERS_TO_CHECK_ROLES_FOR = [ "110462143152803840" ]